
        {/* <div className='nav'>
        <div className='searchbar mb-3'>
        <input type="text" placeholder='search here '
         value={search} onChange={(e)=>setSearch(e.target.value)} className='form-control'/>
        </div>

          
           <Button className='themeBtn'
            variant={theme === "light" ? "dark" : "light"} 
            onClick={toggleTheme}
          >
            {theme === "light" ? "🌙 Dark Mode" : "☀️ Light Mode"}
          </Button> 
        </div> */}


{/* <div className="nav">
  <div className="row align-items-center">
    <div className="col-7">
      <input
        type="text"
        placeholder="search here"
        value={search}
        onChange={(e) => setSearch(e.target.value)}
        className="form-control"
      />
    </div>
    <div className="col-5 text-end">
      <Button
        className="themeBtn"
        variant={theme === "light" ? "dark" : "light"}
        onClick={toggleTheme}
      >
        {theme === "light" ? "🌙 Dark Mode" : "☀️ Light Mode"}
      </Button>
    </div>
  </div>
</div> */}


// function getAllValues(obj) {
//   let values = [];

//   for (let key in obj) {
//     const val = obj[key];
//     if (val && typeof val === "object") {
//       values = values.concat(getAllValues(val)); // go deeper
//     } else {
//       values.push(val);
//     }
//   }

//   return values;
// }

// // filtering with search
// const filterUsers = userData.filter(user =>
//   getAllValues(user).some(value =>
//     value?.toString().toLowerCase().includes(search.toLowerCase())
//   )
// );







// const searchFields = ["name", "email", "phone", "company"]; // jo fields chahiye

// const filterUsers = userData.filter(user =>
//   searchFields.some(field =>
//     user[field]?.toString().toLowerCase().includes(search.toLowerCase())
//   )
// );